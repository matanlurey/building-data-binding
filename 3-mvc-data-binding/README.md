Run a simple local development server:

```dart
pub serve
```

Then visit `localhost:8080` using a web browser.

The first time you visit, it will compile a bit slow. Subsequent compiles are
fast and incremental.
